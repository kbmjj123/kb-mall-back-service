import mock from 'mockjs'
//@ts-ignore
import { tify } from 'chinese-conv'

/**
 * @description 传入当前的语言，然后自动生成另外的语言(目前仅追加繁体字)，
 * @returns 追加自动补全的语言列表
*/
export const mockATitle = (length: number) => {
	
}